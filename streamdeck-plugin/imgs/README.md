# Icons

Action icons should be 72x72 PNG (144x144 for @2x). Files in `actions/` are stubs — replace with solid-colour gradients matching each stat's colour palette before shipping.

Keys are rendered dynamically via `setImage()` at runtime; these icons appear only in the Stream Deck action list.
